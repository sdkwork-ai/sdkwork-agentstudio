# Agent Studio release-2026-04-12-173

## Highlights

- Fixed the packaged desktop documentation intro cards that still rendered raw translation keys in release builds after `release-2026-04-12-172`.
- Added the missing `docs.intro.features.items.*` locale entries to both split locale resources and compatibility aggregate locale bundles:
  - `packages/sdkwork-agentstudio-pc-i18n/src/locales/en/docs.json`
  - `packages/sdkwork-agentstudio-pc-i18n/src/locales/zh/docs.json`
  - `packages/sdkwork-agentstudio-pc-i18n/src/locales/en.json`
  - `packages/sdkwork-agentstudio-pc-i18n/src/locales/zh.json`
- Added a focused regression test so the documentation intro feature cards cannot silently fall back to raw keys again:
  - `packages/sdkwork-agentstudio-pc-i18n/src/docsLocaleRegression.test.ts`

## Carry-Forward Scope

- No unpublished release-note chain is carried forward into this hotfix.
- The previous published GitHub release remains `release-2026-04-12-172`.

## Git Delta Since Last Published GitHub Release

- Last published GitHub release tag: `release-2026-04-12-172`
- Commit range merged into this release: `release-2026-04-12-172..53b39f1`
- Included commits in this range:
  - `53b39f1` fix: restore packaged docs locale feature copy

## Verification Focus

- GREEN:
  - `node --experimental-strip-types packages/sdkwork-agentstudio-pc-i18n/src/docsLocaleRegression.test.ts`
  - `pnpm --dir packages/sdkwork-agentstudio-pc-desktop exec node ../../scripts/run-vite-host.mjs build`
  - `node scripts/verify-desktop-build-assets.mjs`
  - `pnpm --dir packages/sdkwork-agentstudio-pc-desktop exec tauri build --config src-tauri/tauri.windows.conf.json --config <home>/AppData/Local/Temp/tauri-skip-beforebuild-main.json --config src-tauri/generated/tauri.bundle.overlay.json --no-bundle --target x86_64-pc-windows-msvc`
  - WebView2 remote-debug DOM probe against the rebuilt release executable on `/docs` confirmed:
    - suspect key hit count = `0`
    - rendered copy shows localized documentation feature-card text instead of `docs.intro.features.items.*`
- YELLOW / RED:
  - Full NSIS / MSI installer bundling was not rerun in this hotfix loop because the previously documented packaging blockers are still outside the docs locale regression itself.

## Risks And Follow-Up

- This hotfix closes the remaining packaged-app docs locale fallback that users still saw after `release-2026-04-12-172`.
- A later release loop should still address the outstanding Windows installer bundling blockers if installer artifacts are required for publication again.
